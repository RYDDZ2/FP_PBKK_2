// Asumsi: Anda sudah punya utilitas untuk memanggil API
// Ini adalah implementasi mock sederhana untuk membuat kode halaman berfungsi.

export const API_BASE_URL = 'http://localhost:3000/api/v1'; // Sesuaikan dengan port backend Anda

// Asumsi: useAuth() akan memberikan token untuk ProtectedRoute
// Misalnya: const { accessToken } = useAuth();